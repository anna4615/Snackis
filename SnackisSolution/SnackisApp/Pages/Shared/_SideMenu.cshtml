@page

@model IndexModel;

@using Models;
@inject Gateways.ISubjectGateway _subjectGateway;

@{
    List<Subject> subjects = _subjectGateway.GetSubjects().Result.ToList();
}


<h5>Ämnen</h5>

@foreach (var subject in subjects)
{
    <div>
        <a href="@Url.Content($"~/Discussion?id={subject.Id}")">@subject.Name</a>
    </div>
}